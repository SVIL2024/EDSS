# Install a CUDA/CPU-compatible PyTorch build for the target machine first if needed.
torch>=2.0
torchvision>=0.15
numpy>=1.24
pandas>=1.5
scipy>=1.10
scikit-learn>=1.2
Pillow>=9.0
tqdm>=4.65
ftfy>=6.1
regex>=2023.0.0
packaging>=23.0
pytest>=7.0
